profile:
  get:
    description: 유저정보를 가져옵니다.
    operationId: api.rest.user.get_user_profile
    parameters:
      - in: path
        name: user_id
        required: true
        schema:
          type: integer
          minimum: 1

    responses:
      "200":
        description: Success
        content:
          application/json:
            schema:
              $ref: '../models/user.yaml#/user'
