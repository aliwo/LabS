magazines:
    get:
      description: health check
      operationId: api.rest.magazine.get
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  okay:
                    type: boolean
                    example: true
                  magazine:
                    type: array
