users:
  get:
    description: get users
    operationId: admin.rest.user.get_users
    responses:
      "200":
        description: Success
        content:
          application/json:
            schema:
              type: array
